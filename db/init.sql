-- This script assumes that the database is already created:
-- CREATE DATABASE marketplace;

-- Connecting to the database
-- In PostgreSQL, the connection to a database is done via the command line, not within the SQL script itself.

-- the tables themselves will be created in create.sql and seeded in insert.sql
